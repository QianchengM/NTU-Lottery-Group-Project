<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ntu.lottery.mapper.PointsLedgerMapper">

    <insert id="insertLedger">
        INSERT INTO points_ledger
        (user_id, biz_type, biz_id, delta, balance_after, remark)
        VALUES
        (#{userId}, #{bizType}, #{bizId}, #{delta}, #{balanceAfter}, #{remark})
    </insert>

</mapper>
